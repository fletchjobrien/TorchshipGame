# space object

class Space:
 def __init__(self, terrain, structure, coordinate, occupier):
  self.terrain = terrain
  self.coordinate = coordinate
  self.structure = structure
  self.occupier = occupier
