# Weapon obj
class Weapon:
 def __init__(self, name, aoe, range, dmg, knockback, accuracy):
  self.name = name
  self.aoe = aoe
  self.range = range
  self.dmg = dmg
  self.knockback = knockback
  self.accuracy = accuracy
